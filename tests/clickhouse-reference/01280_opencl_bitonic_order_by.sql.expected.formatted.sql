SELECT toUInt8(number * 2) AS x
FROM numbers(42)
ORDER BY x DESC;

SELECT toInt8(number * 2) AS x
FROM numbers(42)
ORDER BY x DESC;

SELECT toUInt16(number * 2) AS x
FROM numbers(42)
ORDER BY x DESC;

SELECT toInt16(number * 2) AS x
FROM numbers(42)
ORDER BY x DESC;

SELECT toUInt32(number * 2) AS x
FROM numbers(42)
ORDER BY x DESC;

SELECT toInt32(number * 2) AS x
FROM numbers(42)
ORDER BY x DESC;

SELECT toUInt64(number * 2) AS x
FROM numbers(42)
ORDER BY x DESC;

SELECT toInt64(number * 2) AS x
FROM numbers(42)
ORDER BY x DESC;