SELECT
    toTypeName(a),
    toTypeName(b),
    toTypeName(c),
    toTypeName(d)
FROM data_null;

SELECT
    toTypeName(a),
    toTypeName(b),
    toTypeName(c),
    toTypeName(d),
    toTypeName(f)
FROM set_null;