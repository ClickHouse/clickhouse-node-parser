SELECT 1
FROM defaults_on_defaults
WHERE length(`Arr.C2`) = 0;

SELECT 1
FROM defaults_on_defaults
WHERE length(`Arr.C3`) = 0;

SELECT 1
FROM defaults_on_defaults
WHERE length(`Arr.C4`) = 0;

SELECT 1
FROM defaults_on_defaults
WHERE ArrLen = 0;

SELECT *
FROM defaults_on_defaults
WHERE ArrLen = 0;