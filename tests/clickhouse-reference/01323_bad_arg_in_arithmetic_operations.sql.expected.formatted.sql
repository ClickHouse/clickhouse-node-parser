SELECT max(multiply(1));

SELECT min(multiply(2));

SELECT sum(multiply(3));

SELECT max(plus(1));

SELECT min(plus(2));

SELECT sum(plus(3));

SELECT max(multiply());

SELECT min(multiply(1, 2, 3));

SELECT sum(plus() + multiply());

SELECT sum(plus(multiply(42, 3), multiply(42)));