-- first value
SELECT first_value(b)
FROM test;

SELECT first_value(b)
FROM test;

SELECT first_value(b)
FROM test;

-- last value
SELECT last_value(b)
FROM test;

SELECT last_value(b)
FROM test;

SELECT last_value(b)
FROM test;