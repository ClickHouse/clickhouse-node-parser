# Tests

## ClickHouse Reference Tests

The `clickhouse-reference.test.ts` suite runs the parser and formatters against (most of) the SELECT statements in [ClickHouse's test suite.](https://github.com/ClickHouse/ClickHouse/tree/master/tests/queries/0_stateless)

For each input file in `tests/clickhouse-reference`, these tests

1. Run `parse()` on the input and compare the output AST to the expected AST.
2. Run `explain()` on the input and compare it to the canonical explain output. This ensures that the parser is parsing all of the information that ClickHouse does.
3. Run `format()` on the AST and compare the output to the expected format.
4. Run `parse()` on the formatted SQL and compare the output AST to the AST from (1). They should match, to ensure that the formatted output contains all of the same information that the original statement contained.

### Related Scripts

- `scripts/filter-select-queries.js` is used to clean up unsupported files and statements from the original test input set.
- `scripts/generate-expected-outputs.ts` is used to generate the expected outputs for each input file. Run this when changing formatting or AST output.
  - Use `npm run generate:expected-outputs` to run this script
  - Updates to expected outputs should be persisted in git, so that reviewers can be aware of changes to parser and formatter outputs.
  - By default, this script will not generate any EXPLAIN outputs for inputs that already have EXPLAIN outputs (since these are reference outputs generated by ClickHouse). When adding a new test case, run `docker compose up` prior to running this script, so that the script can use a running docker ClickHouse container to run `EXPLAIN AST`.

## Query Params Tests

The `query-params.test.ts` tests parsing and formatting of query parameters. ClickHouse EXPLAIN AST requires substitutions to be set for each query parameter, so the reference tests don't cover these cases.