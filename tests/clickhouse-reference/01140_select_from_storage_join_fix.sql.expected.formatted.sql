SELECT
    *,
    toTypeName(id),
    toTypeName(name)
FROM t1;

SELECT
    *,
    toTypeName(id),
    toTypeName(name)
FROM t2;

SELECT
    *,
    toTypeName(id),
    toTypeName(name)
FROM t3;

SELECT
    *,
    toTypeName(id),
    toTypeName(name)
FROM t4;